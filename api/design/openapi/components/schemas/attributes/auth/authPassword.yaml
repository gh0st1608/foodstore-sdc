type: object
description: Autenticación usando usuario y contraseña.
required:
  - authType
  - email
  - password
properties:
  authType:
    $ref: ./typeAuth.yaml  # debe contener enum: [password]
  email:
    $ref: ../../attributes/email.yaml
  password:
    $ref: ./password.yaml
  captchaToken:
    type: string
    description: Token de reCAPTCHA (opcional, si se requiere validación anti-bot)
    example: "03AGdBq24..."
