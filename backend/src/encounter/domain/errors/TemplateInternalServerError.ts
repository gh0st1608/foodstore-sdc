/* import { HttpStatusResponse } from '../../../common/domain/constants/HttpStatusResponse';
import { CustomError } from '../../../common/domain/errors/CustomError';
import { EncounterDomainErrorMessages } from '../constants/EncounterDomainMessages';

export class EncounterInternalServerError extends CustomError {
  constructor(message: string) {
    super(HttpStatusResponse.INTERNAL_SERVER_ERROR, message);
  }
}
 */